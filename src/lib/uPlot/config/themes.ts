import { ChartTheme } from "../types";

export const defaultTheme: ChartTheme = {
  gridColor: "var(--chart-grid)",
  axisColor: "var(--chart-axis)",
  tickColor: "var(--chart-tick)",
  backgroundColor: "var(--chart-background)",
};
